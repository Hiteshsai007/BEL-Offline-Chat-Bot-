# BEL Offline AI Interface — Python Dependencies
# Install offline: pip install --no-index --find-links=./wheels -r requirements.txt
# Or online (first-time setup only): pip install -r requirements.txt

fastapi==0.140.0
uvicorn[standard]==0.51.0
faiss-cpu==1.14.3
sentence-transformers==5.6.1
pdfplumber==0.11.10
pymupdf==1.28.0
httpx==0.28.1
pyyaml==6.0.3
python-multipart==0.0.32
